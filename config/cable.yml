development:
  adapter: async

test:
  adapter: test

production:
  # Para esta primera etapa de produccion evitamos depender de Redis
  # mientras la app no use canales en tiempo real.
  adapter: async
  channel_prefix: app_production
