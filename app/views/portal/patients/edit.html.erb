<% content_for :title, "Editar paciente — Portal" %>

<section class="rounded-2xl border border-sarah-border bg-white/90 p-8 shadow-sm">
  <h1 class="font-serif text-2xl font-semibold text-sarah-ink">Editar paciente</h1>
  <p class="mt-2 text-sm text-sarah-muted"><%= @patient.full_name %></p>

  <div class="mt-6">
    <%= render "form", patient: @patient %>
  </div>
</section>
